{
  "buildCommand": "pip install -r requirements.txt",
  "outputDirectory": ".",
  "framework": "fastapi",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    }
  ]
} 